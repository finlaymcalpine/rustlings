// variables6.rs
//
// Execute `rustlings hint variables6` or use the `hint` watch subcommand for a
// hint.

const NUMBER: u32 = 3; // when we define a constant, we need to give a type declaration. doesn't infer type
fn main() {
    println!("Number {}", NUMBER);
}
